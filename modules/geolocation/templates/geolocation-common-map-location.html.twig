<div class="geolocation" data-lat="{{ position.lat }}" data-lng="{{ position.lng }}" {% if icon is not empty %} data-icon="{{ icon }}" {% endif %}>
    <h2>{{ title }}</h2>
    <div>{{ content }}</div>
</div>
